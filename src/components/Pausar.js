
import { Button } from "@mui/material"

export default function Pausar() {
    return (
        <div>
            <h4>Pausar</h4>
            <Button variant="contained">||</Button>
        </div>
    )
}